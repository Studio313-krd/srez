# Проверка «Среза» · 14.09.2026

Сервис развёрнут на https://srez.studio313.ru/ в стэке Portainer `srez`, ID 102, окружение 3. Данные перенесены из изолированного локального проекта `srez-preview`.

## Данные и доступы

- В рабочем интерфейсе 83 магазина, 200 сотрудников, 7 юрлиц, 2 595 импортированных планов, 1 370 импортированных срезов с показателями и архив 89 исходных листов. Четыре старых демонстрационных магазина архивированы; их история сохранена.
- База целиком содержит 2 603 плана и 1 381 версию с учётом архивных демонстрационных данных. При переносе значения не менялись.
- Один общий активный администратор офиса и 83 отдельных аккаунта магазинов. Первичные пароли сохранены только в закрытом локальном файле, вне Git.
- Через HTTPS проверены реальный вход администратора и вход одного магазина. Магазину возвращается ровно одна точка; управление и экспорт недоступны (403), чужой отчёт недоступен (404). Secure и HttpOnly у сессионной cookie подтверждены.

## Код и интерфейс

- TypeScript/Vite и Docker-образ успешно собраны, миграции по `0005` включительно применены.
- Полный набор из 52 серверных тестов прошёл на PostgreSQL. После добавления выдачи доступов и доверенных прокси прошли 11 целевых тестов на PostgreSQL (два новых сценария и девять проверок управления). Всего проверены 54 сценария, несколькими запусками.
- Проверены полнота и повторяемость импорта, версии и конфликты исправления отчётов, ограничения ролей, управление справочниками, графики, вопросы и ответы, обе очереди уведомлений, повторы и сокрытие секретов. Ответы API мессенджеров в тестах имитируются.
- Chrome на учебном стенде: удаление ссылки «Замечания», сворачивание панели с сохранением состояния, совместный поиск по нескольким столбцам, числовая сортировка, сброс, выгрузка CSV и XLSX. Проверены таблицы магазинов, сотрудников, планов и истории. Содержимое файлов повторно прочитано: числовые значения и пустые ячейки сохранены, формулы из текста не исполняются.
- Chrome на рабочем HTTPS-сайте: 83 магазина, 200 сотрудников, 84 активных аккаунта. За 13 сентября на закрытие фильтр «Требуют внимания» показывает 43 магазина: 42 без отчёта и один неполный. Поиск по столбцу и реальный Excel-экспорт прошли; ошибок JavaScript нет.
- На рабочем сайте поиск нашёл сотрудника за пределами первой сотни и строку 70 исходного листа, хотя на странице показываются 50 строк. В «Доступы магазинов» проверено наличие всех 83 строк.
- До выкладки на импортированной базе проверены девять открытий одного неполного отчёта: три на 13:00, три на 17:00, три на закрытие. Заголовок и показатели соответствуют выбранному времени. Основной текст 16 px, подписи 14 px. На ширине 390 px страница не переполняется; широкие таблицы прокручиваются внутри блока.
- Обновлены и визуально проверены после рендеринга все девять страниц PDF: шесть для видеоконтроля и три для продавцов. Скриншоты используют отдельные учебные данные.

## Развёртывание и копии

- db, web, worker и backup запущены; миграция завершилась с кодом 0. Healthchecks исправны.
- Устранён цикл HTTPS-перенаправлений: приложению задан фактически наблюдаемый адрес nginx через Docker gateway. Прямой запрос на HTTP-порт с подставленным заголовком HTTPS перенаправляется на HTTPS.
- Перед переносом локальная копия восстановлена в отдельную временную БД. После выдачи 83 доступов создана копия на сервере и скачана локально: 1 197 296 байт. Она также восстановлена в отдельную БД; подтверждены 84 активных аккаунта, 83 магазина и 1 381 версия. Временная БД удалена, рабочая не перезаписывалась.
- Служба backup создаёт копию каждые 24 часа в отдельном томе, хранение 14 дней. Независимое внешнее хранилище и внешний мониторинг пока не настроены.

## Что остаётся владельцу и офису

Подтвердить реальные графики сегодня для контроля сроков завтра, проверить планы, распределить доступы и согласовать переход магазинов на сервис. Новые данные ведутся здесь; автосинхронизации с Google нет.

Telegram-бот `@srez_notify_bot` создан, токен указан в Portainer. Группа будет подключена владельцем позже, отправка выключена. Проверка прямого соединения с текущим адресом Telegram API завершилась тайм-аутом, тогда как соединения с GitHub и MAX доступны; это сетевое ограничение, а не подтверждение неверного токена. Реальная доставка не проверена. MAX-бот пока не создан. Подключение описано в [BOTS.md](deploy/BOTS.md).

Регулярные сводки, сверка с кассой и постоянные офлайн-черновики в этот этап не входят. Успешные технические проверки не заменяют первую рабочую смену с сотрудниками.

## Тестовая версия · 16.09.2026

Подготовлены отдельная ветка `test`, `compose.test.yaml`, порт 9992 и база `srez_test`. Рабочий стэк ID 102 не изменялся. Тестовые команды запрещены в обычной конфигурации; короткий прогон разрешён только для двух помеченных учебных магазинов. Первый запуск создаёт новые случайные пароли, рабочие данные не копирует.

- Полный набор **59 серверных тестов** прошёл на PostgreSQL в отдельном Docker-проекте `srez-acceptance-check`. Проверены изоляция, роли, шесть сроков, отсутствие автоматического прогона на следующий день, подтверждение перезапуска, очередь и тестовая отметка сообщения. Внешний Telegram API в этих тестах имитируется.
- TypeScript/Vite собраны. В двух локальных браузерных сессиях проверены вход администратора и продавца двух магазинов, создание и подтверждённый перезапуск прогона, своевременная и поздняя сдача, появление пропуска, вопрос и ответ, ошибка накопительного итога, исправление с сохранением двух версий и заполнение всех шести отчётов.
- Исправлено автоматическое открытие заранее открытой формы при наступлении времени сдачи. В браузере с управляемыми часами подтверждены блокировка до срока и разблокировка без закрытия карточки; серверное время и данные при этой проверке не менялись.
- Созданы и после рендеринга визуально проверены все семь страниц двух PDF первого запуска: четыре страницы для видеоконтроля, три для продавца. Скриншоты содержат только учебные данные.

Тестовый стэк развёрнут на **https://srez-test.studio313.ru/**, Portainer ID **103**, порт **9992**. Созданы отдельные PostgreSQL, том и сеть. Для HTTPS указан наблюдаемый адрес nginx `192.168.48.1/32` и loopback. Вход обеих ролей, Secure/HttpOnly тестовой cookie, сохранение своевременного отчёта, автоматический пропуск, поздняя сдача, вопрос, ответ и исправление с двумя версиями проверены уже на сервере. Продавцу управление и перезапуск возвращают 403. На ширине 390 px нет горизонтального переполнения. CSV и XLSX реально скачаны.

Заключительная проверка: все шесть отчётов двух магазинов сохранены, обе выгрузки повторно прочитаны и содержат по две строки. Web, worker и PostgreSQL имеют healthy, миграция завершена с кодом 0. Рабочий и тестовый HTTPS health endpoints отвечают 200. На тестовом сервере версия приложения `7d90224`; последующие изменения PDF и журнала проверок не требуют пересборки приложения. Для нового совместного прогона администратор нажимает «Начать заново».

Дополнительно прошли четыре проверки подключения ботов: отдельный чат определяется только по `my_chat_member`, без сообщения или `/start`. Токен подтверждён Telegram с компьютера владельца; серверное подключение к API по IPv4 завершается тайм-аутом при доступных GitHub и MAX. Последнее чтение событий бота вернуло пустой список. Отправка выключена до получения ID группы и рабочего сетевого маршрута, реальная доставка ещё не подтверждена. Локальный стенд принудительно отключает отправку наружу.
